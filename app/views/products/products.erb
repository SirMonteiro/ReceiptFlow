<div style="font-family: Arial, sans-serif; max-width: 800px; margin: 20px auto; padding: 20px; border: 1px solid #ddd; border-radius: 8px;">

  <h1 style="border-bottom: 2px solid #eee; padding-bottom: 10px;">Product Importer</h1>

  <!-- Flash Messages for success/alert -->
  <% if flash[:notice] %>
    <div style="padding: 15px; background-color: #d4edda; color: #155724; border: 1px solid #c3e6cb; border-radius: 4px; margin-bottom: 15px;">
      <%= flash[:notice] %>
    </div>
  <% end %>
  <% if flash[:alert] %>
    <div style="padding: 15px; background-color: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; border-radius: 4px; margin-bottom: 15px;">
      <%= flash[:alert] %>
    </div>
  <% end %>

  <h2>Upload XML File</h2>
  <p>Upload an XML file with product data to import it into the database.</p>

  <!--
    This is the upload form.
    - We use `form_with` targeting our `import_products_path` (which we'll need to create in routes.rb)
    - method: :post is standard
    - `multipart: true` is ESSENTIAL for file uploads.
  -->
  <%= form_with(url: import_products_path, method: :post, multipart: true, local: true) do |form| %>
    <div>
      <%= form.label :xml_file, 'Select XML File:' %>
      <%= form.file_field :xml_file, accept: '.xml', style: "margin-top: 10px; display: block;" %>
    </div>

    <div style="margin-top: 20px;">
      <%= form.submit 'Upload and Import Products', style: "padding: 10px 15px; background-color: #007bff; color: white; border: none; border-radius: 4px; cursor: pointer;" %>
    </div>
  <% end %>

  <h2 style="border-top: 2px solid #eee; padding-top: 20px; margin-top: 30px;">Current Products in Database</h2>

  <table style="width: 100%; border-collapse: collapse; margin-top: 15px;">
    <thead>
      <tr style="background-color: #f9f9f9;">
        <th style="border: 1px solid #ddd; padding: 8px; text-align: left;">Name</th>
        <th style="border: 1px solid #ddd; padding: 8px; text-align: left;">Price</th>
        <th style="border: 1px solid #ddd; padding: 8px; text-align: left;">Description</th>
      </tr>
    </thead>
    <tbody>
      <% @products.each do |product| %>
        <tr>
          <td style="border: 1px solid #ddd; padding: 8px;"><%= product.name %></td>
          <td style="border: 1px solid #ddd; padding: 8px;">$<%= product.price %></td>
          <td style="border: 1px solid #ddd; padding: 8px;"><%= product.description %></td>
        </tr>
      <% end %>
      <% if @products.empty? %>
        <tr>
          <td colspan="3" style="border: 1px solid #ddd; padding: 8px; text-align: center; color: #777;">No products in database yet.</td>
        </tr>
      <% end %>
    </tbody>
  </table>

</div>
